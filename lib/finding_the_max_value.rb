def find_max_value(array)
  sorted_array = array.sort
  return sorted_array[-1]
end

=begin
#this is the easiest way to do it but they want you to do it manually(see above)
def find_max_value(array)
  sorted_array = array.sort
  return sorted_array[-1]
end
=end